# Heavy data parsing through NodeJS

## Install dependencies
`$ npm install`

## Run the application
`$ npm run start`

## API request
`http://localhost:3000`

### Check converted file
`/data/records_yaml.yaml`